replicaCount: 1

image:
  repository: ghcr.io/heremias/test-app-test-main
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "b1227cfee3a8143009a9338f9d2fcc30a79b2f85"

imagePullSecrets:
  - name: redwoods

service:
  type: LoadBalancer
  port: 8080
  targetPort: 80

configMap:
  name: reactconfig

ingress:
  enabled: false
