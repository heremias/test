replicaCount: 1

image:
  repository: ghcr.io/heremias/test-app-test-main
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "c1f9bee77f2bc86a00153f292b0268c4e0f94c6e"

imagePullSecrets:
  - name: redwoods

service:
  type: LoadBalancer
  port: 80
  targetPort: 8080

configMap:
  name: reactconfig

ingress:
  enabled: false
