replicaCount: 1

image:
  repository: ghcr.io/heremias/test-app-test-main
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "cc0e92dc5fde33b38e9e424c1b668b5833ff807c"

imagePullSecrets:
  - name: redwoods

service:
  type: LoadBalancer
  port: 80
  targetPort: 80

configMap:
  name: reactconfig

ingress:
  enabled: false
