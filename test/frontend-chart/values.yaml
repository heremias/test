replicaCount: 1

image:
  repository: ghcr.io/heremias/test-app-test-main
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "00ec160f4184c665a72a16809e6a105ff52aa6ae"

imagePullSecrets:
  - name: redwoods

service:
  type: LoadBalancer
  port: 8080
  targetPort: 80

configMap:
  name: reactconfig

ingress:
  enabled: false
