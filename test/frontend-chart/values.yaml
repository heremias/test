replicaCount: 1

image:
  repository: ghcr.io/heremias/test-app-test-main
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "75aca8fac196f5b2b6fbe7c70ce7fe654893c366"

imagePullSecrets:
  - name: redwoods

service:
  type: LoadBalancer
  port: 80
  targetPort: 8000

configMap:
  name: reactconfig

ingress:
  enabled: false
