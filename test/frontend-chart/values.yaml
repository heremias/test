replicaCount: 1

image:
  repository: ghcr.io/heremias/test-app-test-main
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "0185d1fb6d4f176b8bc352f1682b6f2662a7ab5c"

imagePullSecrets:
  - name: redwoods

service:
  type: LoadBalancer
  port: 80
  targetPort: 8080

configMap:
  name: reactconfig

ingress:
  enabled: false
